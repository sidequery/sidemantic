[package]
name = "dax-parser"
version = "0.1.0"
edition = "2021"
description = "DAX lexer+parser"
license = "AGPL-3.0-only"
repository = "https://github.com/sidequery/sidemantic"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
