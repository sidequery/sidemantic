models_dir: models

connection:
  type: duckdb
  path: data/warehouse.db

# Pre-aggregation configuration
preagg_schema: preagg  # Store in 'preagg' schema

# Note: CLI query routing is explicit; pass --use-preaggregations to opt in.
# The schema above tells the system where to find/create pre-agg tables.
