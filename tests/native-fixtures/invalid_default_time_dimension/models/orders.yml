version: 1

models:
  - name: orders
    table: orders
    primary_key: order_id
    default_time_dimension: created_at
    dimensions:
      - name: created_at
        type: categorical
    metrics:
      - name: order_count
        agg: count
