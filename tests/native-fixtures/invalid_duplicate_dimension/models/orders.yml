version: 1

models:
  - name: orders
    table: orders
    primary_key: order_id
    dimensions:
      - name: status
        type: categorical
      - name: status
        type: categorical
    metrics:
      - name: order_count
        agg: count
