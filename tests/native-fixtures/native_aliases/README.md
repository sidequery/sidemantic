# Native Compatibility Aliases

Verifies that Python compatibility input aliases are accepted by both native runtimes:

- model `measures` as an alias for `metrics`
- dimension and metric `expr` as aliases for `sql`
- metric `measure` as an alias for `sql`
- model `auto_dimensions: false` as accepted compatibility input
