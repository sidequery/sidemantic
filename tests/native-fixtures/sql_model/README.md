# SQL Model

Proves that a native YAML model can use a SQL query body as its source, that the source query is wrapped safely by generated SQL, and that dimensions and metrics still resolve against the SQL model output.
